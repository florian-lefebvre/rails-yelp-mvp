<h1>Restaurants</h1>
<%= link_to "Add new restaurant", new_restaurant_path, class: "btn btn-primary" %>
<% @restaurants.each do |restaurant| %>
  <ul>
    <li><%= link_to restaurant.name, restaurant_path(restaurant) %> (<%= restaurant.reviews.length %> reviews)</li>
  </ul>
<% end %>
